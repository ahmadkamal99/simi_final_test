import 'package:flutter/material.dart';

class edit_component extends StatelessWidget {
  const edit_component({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
